# 00_GAME_DESIGN.md — 游戏设计（总控 + 玩法 + 系统规格）

> 本文件由原 00_MASTER_PROMPT / 01_GAME_DESIGN / 02_SYSTEM_SPEC / 04_OPEN_QUESTIONS 合并而成（2026-09-15）。
> 美术规范见 `01_ART_GUIDE.md`；架构、战斗实现与待办见 `02_TECH_BUILD.md`。

---

# 第一部分 · 总控约束

## 项目名称
Keep Quiet / 嘘（2026-09-15 定稿；工程目录名仍为 SteamPunkExtraction）

## 一句话定位
一款蒸汽朋克风格的"搜打撤"游戏（逻辑 2D、渲染 3D 的 HD-2D 双轨制）：
每局程序化生成地图，玩家搜刮资源，在限定时间内找到撤离点逃离，
成功带出的资源用于局外永久养成。

## 产品定位（2026-09-15 定）
* 单机离线，无多人/PvP，无任务系统（纯自由搜刮）。
* 无尽循环、无通关；预期养成毕业节奏约 50 小时。
* 一局 60 分钟为**真实 60 分钟**（硬核向）；`debug.time_scale` 仅供测试。
* 发布平台：Steam。

## 技术栈
- 引擎：Godot 4.x
- 语言：GDScript
- 版本控制：Git
- 美术：AI 生成 + 人工筛选/修正
- 音频：免费音效库 + 简单剪辑

## AI 协作铁律
1. 每次对话前，先阅读本目录下所有 .md 文件。
2. 发现设计不清楚时，写入本文件第四部分「待回答」，不要擅自改核心方向。
3. 不许删除或替换已确认的核心玩法、技能、系统，除非我在对话中明确同意。
4. 代码改动后必须能编译通过；报错自行读取日志修复。
5. 所有数值配置写入 Data/*.json，不要硬编码在脚本里。
6. 美术素材生成必须参考 01_ART_GUIDE.md，一次只生成一个类别。

## 当前阶段与里程碑
Phase 1：垂直切片（进行中）

- [x] Phase 0 准备期 —— 2026-09-13 完成：项目骨架、三总管家、烟雾测试通过
- [ ] Phase 1 垂直切片（4-6 周）
- [ ] Phase 2 美术与内容填充（4-8 周）
- [ ] Phase 3 系统深化（4-6 周）
- [ ] Phase 4 打磨与发布（4-8 周）

---

# 第二部分 · 核心玩法

## 核心循环
进图 → 搜刮资源 → 限时撤离 → 局外养成 → 再进图

## 设计支柱
1. **风险决策**：带什么进去、搜多少、什么时候撤。
2. **紧张感**：时间压力 + 敌人威胁，制造"贪婪 vs 恐惧"的拉扯。
3. **稳定成长**：局外养成让每局都有积累，失败也有收获。

### 游玩节奏与难度（2026-09-16 定）
* **前期潜行、后期战斗**：开局肯定以潜行为主（游戏名 Keep Quiet 的玩法承诺），
  发育起来后转为战斗玩法。配套：给玩家加**下蹲/静走状态**（降低脚步噪音，待实现）。
* **难度靠撤离点控制**：45 分钟前相对容易，之后越来越难。

## 一局游戏的流程
1. 玩家在基地选择携带装备/消耗品（局外准备，**携带系统待实现**）。
2. 进入程序化生成的地图。
3. 在地图中搜刮资源点，击败/躲避敌人。
4. 倒计时结束前到达撤离点，站够 N 秒完成撤离。
5. 成功：资源带回基地，进入局外养成。
   失败：死亡或超时，丢失本局未撤离的资源。

## 局内时长与阶段（已确认）
一局共 60 分钟，分四个阶段：

| 阶段 | 时间 | 撤离点 |
| :--- | :--- | :--- |
| 一 · 搜刮期 | 0-30 分钟 | 无 |
| 二 | 30-45 分钟 | 随机开启 3 个 |
| 三 | 45-55 分钟 | 随机关闭 1 个（剩 2） |
| 四 | 55-60 分钟 | 再关 1 个（剩 1，最后 5 分钟仅此一个） |

- 60 分钟超时 = 死亡（与战斗死亡同罚：物资全部丢失）。
- 全部数值在 Data/config.json 的 session / extraction 节点。

## 资源设计（6 种，2026-09 已定）
| 资源 | 稀有度 | 用途 |
| :--- | :--- | :--- |
| 木头 | 普通 | 升级货币（背包/撤离速度线） |
| 石头 | 普通 | 升级货币（获取线/仓库容量） |
| 铁 | 普通 | 升级货币（生命线） |
| 金币 | 稀有 | 高级升级货币 |
| 石油 | 稀有 | 高级升级货币（稀有资源率） |
| 食物 | 普通 | 战斗/生存：局内每 60 秒自动消耗 1 个；按 H 手动吃 1 个回 25 血；断粮则持续掉血（见 3.9） |

- 每个资源点拾取获得 10 单位（loot.amount_per_node）
- 击败敌人也会掉落（enemy.drop）：75% 概率，2~6 单位，种类按权重随机（见 3.6）
- 局内背包：每种物品占 1 格，容量默认 10 格（backpack_capacity 可升级）
- 仓库（局外）：默认 20 格，每种物品叠加上限 1000，容量可升级（warehouse_capacity）
- 升级费用 = **多资源混合配方**（2026-09-15 定）；资源另有消耗性用途（造装备/建筑等）
- 资源 `value` 字段暂无消费方，去留见「待回答」第 21 条

## 失败惩罚与积分（已确认）
- 死亡与超时相同：丢失本局全部未撤离资源 + **带入的初始装备**（塔科夫式全丢，硬核向）。
- 主动放弃本局 = 按失败处理。
- **积分货币**（落实设计支柱 3"失败也有收获"，**待实现**）：杀怪 + 生存获得积分；
  死亡物资全掉，但积分保留，可用于升级。
- **积分结算规则**（2026-09-16 定，堵死"故意送死变现"）：死亡只按**很低的分数**入账；
  **成功撤离积分翻倍**。生存给分细则与双货币面板划分见「待回答」第 27 条。
- **安全箱**：初始没有，后续通过道具开启（死亡仍保留少量格子）；与背包两套格子不冲突。
  开启道具的掉落源待设计（用户：会考虑提前加）。
- **保底初始装**（2026-09-16 定）：基地保底发放一套初始装备，防止"穷死循环"。
- **死亡回收 =「失物怪」机制**（2026-09-16 定，替代"重进同图/撒掉落物"两种方案，暂不排期实现）：
  死亡丢失的背包+带入装备**不直接撒到地图上**，而是生成一只**特殊怪物**（暂名"失物怪"）
  在之后的对局地图上刷新。
  * **归还方式 = 掉血随机掉落**（2026-09-16 定，替代"击败才归还"）：失物怪**每次损失一定比例 HP
    就随机掉落一份遗失物**到地上（走现有掉落物拾取管线），**不必击杀**；击败则掉落全部剩余。
    打多少拿回多少——追不上/打不死也能部分止损，同时"穷追猛打"有额外回报。
    不需要单局保存/种子重放——死亡仍是全丢，但"找回"变成一局内的狩猎目标。
  * **失物怪行为 = 随机两种状态**（2026-09-16 定，刷出时随机其一，玩家需试探才知道是哪种）：
    * **怯懦型**：HP > 50% 正常交战，**HP ≤ 50% 时开始逃跑**（先打后追，残血追击战）；
    * **凶悍型**：**HP > 50% 时就逃跑**（一见面就跑，逼你先追打消耗），
      **HP ≤ 50% 反而停下战斗**（残血反扑，防止无脑风筝）。
    两种状态让"取回"每局都是不同的追击/围剿体验。
  * 待设计：出现在下一局还是每局概率刷、多次死亡是否各刷一只、未击败是否永久保留、
    是否掉全部丢失物。见「待回答」第 48 条。

## 局外养成两条线
| 线 | 内容 | 对局内作用 |
| :--- | :--- | :--- |
| 生存线 | 生命、背包容量、撤离速度 | 能带更多、走更快 |
| 获取线 | 资源发现概率、稀有资源出现率 | 搜到更好的东西 |

## 世界观（2026-09-15 定）
* 玩家是**拾荒者**。
* 敌人两条来源线都有：机械（故障游荡）与丧尸（瘟疫/改造失败）。
* 撤离点视觉主题随机：飞艇 / 矿车 / 地下铁。
* 地图"残骸"装饰目前是纯贴图（暂不做叙事载体）。

## 参考坐标
- 核心循环参考：《逃离塔科夫》《逃离鸭科夫》
- 美术风格参考：《They Are Billions》

---

# 第三部分 · 系统规格

## 3.1 地图生成

* 尺寸：原型阶段 128x128；方向 = **尽量大，前提是不卡**（性能优先于尺寸目标值）
* 生成方式：FastNoiseLite 噪声生成地形（阈值以上=墙），中心 5x5 强制出生区
* 边界：地图四周强制 1 圈外墙（与基地一致），玩家不可能跑出地图范围
* 连通性保障（已实现）：生成后从出生点洪水填充得出可达区域；
  可达地板占比 < map.min_reachable_ratio（30%）时换种子重新生成（最多 max_regen_attempts=10 次）；
  撤离点与敌人只刷在可达格内 → 出生点与每个撤离点之间必然走通
* 导航网格（已实现）：基于 walls 网格为每个地板格生成可通行多边形，挂载 NavigationRegion2D；
  玩家点击地面时调用 NavigationServer2D.map_get_path 自动避墙寻路
* 资源点：随机放置，密度可配置（已实现，见 3.2；同样只刷可达格）；
  **只在开局刷新一次**，局内不再新增
* 撤离点：由 ExtractionSystem 调度（见 3.4 时间轴）
* 敌人：由 EnemySystem 生成（见 3.6）
* 群系（已实现，数据驱动 config.map.biomes）：草地/荒原/森林/雪原，权重分段 + 噪声分位数定边界；
  **群系有玩法差异**：各群系资源产出不一样（荒原多矿、森林多树等）
* 地形速度网格（已实现）：雪原 0.62、河流浅滩 0.72；**对敌人同样生效**
* 河流（调参中）：普通移动**不可涉水**，只有特定能力/道具能过；0.72 减速是"岸边泥地/浅滩"，不会淹死
* 裂缝：无法直线穿过（阻断通行，需绕行或跳跃类能力）
* 矿脉（iron/gold/oil veins）：**可反复采集节点**，限定荒原群系产出
* 天气系统（**待做**）：影响噪音——下雨降低声音传播范围，打雷提高噪音阈值（掩盖玩家动静）；
  **局内动态切换**（不是整局固定一种），且有**道具可改变/利用天气**（2026-09-16 定）
* POI / 地图建筑 / 巢穴（**待做**）：巢穴预期产出不同类型怪物，有拆除奖励
* 玩家**不能**在地图上放置自定义标记

## 3.2 搜刮系统（已实现）

* 交互方式：**自动拾取**——玩家移动进入拾取半径（loot.pickup_radius_px，默认 20px）即自动获得 10 单位（loot.amount_per_node），资源点消失；背包格满时拾取失败，冷却 loot.pickup_retry_seconds（0.5 秒）后自动重试
* **自动拾取 ≠ 搜刮**（2026-09-15 定）：搜刮 = 拆建筑/采集，是另一套动作；手感清单"搜刮时减速"针对后者
* 采集玩法（**待做**）：树/石/矿脉可反复采集（`ResourceRegistry.harvest()` 需接入口）；后续基地建筑可自动采集
* 资源点生成（loot 节点）：占可达地板格的 density（5%）比例，只刷可达格；每个点随机绑定一种资源（按 rarity 加权：common 3 / rare 1）
* **资源点总量会被刻意压低**（2026-09-16 定）：后期密度会调小、随机少量投放，
  定位是"给玩家的惊喜"而非稳定收入；稳定收入靠采集/基地发展（见 3.13）。
  这是"风险决策"支柱不失效的前提。
* 资源种类（resources 节点）：木头/石头/铁/金币/石油/食物，各带名称、稀有度、价值、占位颜色
* 局内背包：每种物品占 1 格；物品种类数 ≤ backpack_capacity（默认 10，局外升级）；格满时新种类拾取失败（资源点保留），已有种类可继续叠加
* HUD 左下角常驻背包栏（种类数/容量 + 明细）
* 食物用途（2026-09-14 用户定：食物是战斗资源）：局内每 60 秒自动消耗 1 个；
  背包无食物则进入饥饿状态，每 30 秒扣 5 血；按 H 主动吃 1 个回 25 血（冷却 1 秒）。
  实现在 survival_system.gd，数值在 config 的 survival 节点（详见 3.9）
* 资源点显隐由雾系统控制（视野内才可见，同敌人规则）
* 仓库入库规则（storage 节点）：撤离成功自动入库；默认 20 格（warehouse_capacity 可升级 +5/级），每种物品叠加上限 1000，超出截断丢弃、仓库满则新种类整组丢弃（日志提示）
* 仓库升级线保留：后续会加入更多道具种类，格子会不够用
* 敌人掉落物品（装备/道具类）**待做**，当前只掉资源

## 3.3 限时系统

* 局内倒计时 60 分钟（session.time_limit_seconds = 3600），HUD 顶部常驻显示
* debug.time_scale 可加速倒计时用于测试（只加速计时，不加速玩家）；**正式玩之前必须调回 1.0**
* 时间到 → 判定超时失败（与死亡同罚：物资全丢，积分保留）

## 3.4 撤离系统（已实现）

* 时间轴（extraction 节点，全部可配置）：
  * 30 分钟：随机开启 3 个撤离点
  * 45 / 55 分钟：各随机关闭 1 个；最后一个开放至超时
* 选址：只刷在地板格，距出生点 ≥ min_distance_from_spawn_cells，彼此 ≥ min_distance_between_points_cells；采样失败过多自动放宽
* **与敌人距离不做限制**（2026-09-15 定）：可能刷在怪堆旁，形成守卫战
* 关闭顺序：撤离点生成时即洗牌预定（不是到点才随机）；到点按预定顺序关闭
* 关闭保护：轮到关闭的点若玩家正在圈内，改关预定顺序中的下一个；全部都有玩家在场则本轮作废
* 小地图（extraction.minimap 节点）：
  * 撤离点开启时（30 分钟）左上角弹出，按比例呈现全图地形，显示 duration_seconds（60 秒）后消失；**错过不做额外补偿**
  * 每次关闭前 warn_before_close_seconds（60 秒）再次弹出，并将即将关闭的撤离点标为警示橙圈闪烁
  * 玩家=绿点（实时位置）、开放撤离点=白点；尺寸 size_px 可配置
* 撤离方式：进入圆形触发区域站够 extraction_hold_seconds（3 秒），离开重置；
  **被敌人攻击会打断并重置进度**（2026-09-15 定）
* 每个撤离点**只能用一次**（撤离成功即消耗）。
  注（2026-09-16 澄清）：单机下这条**不是为了抢点**，而是为后续**"抓宠撤离"**铺路——
  玩家会拥有**可控单位**（捕捉的怪物），撤离点要连它们一起带走。见 3.13
* 撤离进度弧有脉冲动画（手感清单 ✓）
* 成功/失败分支接入 RunManager（extract / player_died / timeout）
* 撤离点关闭前吸引怪物聚集（之前讨论的设计）：**还没做**

## 3.5 局外养成与基地（已实现）

* 存档系统：user://save.json（资源仓库 bank + 升级等级）；**多档位存档**（待实现）
* 基地（base 节点）：64x64 固定平地+外圈墙；建筑每栋 4x4 格，布局手工配置（cell=左上角格）；
  后续**建筑可自由摆放**、慢慢扩建（工坊、NPC 住所等）
* 建筑（靠近按 E 交互，按住不重复触发）：
  * 仓库（warehouse）：查看资源库存面板
  * 雕像（statue）：局外升级面板——两条线（生存/获取），显示名称/等级/当前值/费用，按钮购买即时生效并存档
  * 出发大门（gate）：按 E 进入一局
* 流程：启动进基地 → 大门进局 → 撤离/死亡/超时结算（按 R 回基地）→ 雕像升级 → 再进局
* 面板打开时游戏暂停（process_mode=ALWAYS 保持面板可交互）
* 升级项配置：meta_progression.\*.{name, format, base, per_level, max_level, cost}；费用=cost×(当前等级+1)
* NPC（商人/任务发布者）：后续慢慢加

## 3.6 敌人与视野（已实现）

* 敌人生成（enemy 节点）：每局 count（100）个（先固定，后续再定）；只刷在地板格；
  距玩家出生点 ≥ min_distance_from_player_cells（20 格）；位置不重复（合法格洗牌抽取）
* 正式版**约 6 种怪**（在做）：按**地形分布**刷不同怪（映射表后定，见「待回答」第 49 条）；
  机械派 vs 丧尸派**会互相攻击**（待做），玩家在场时**谁近打谁**（2026-09-16 定，仇恨机制后续再细化）；
  有**远程攻击**敌人（待做）；玩法差异见「待回答」第 23 条
* 敌人 AI（2026-09-14 用户定：巡逻 + 追击，已实现）：
  * Patrol 巡逻：在出生点周围 patrol_radius_cells（6 格）内随机选可达点走过去，到达后停留 patrol_idle_seconds（1.5 秒）
  * Chase 追击：视野 vision_cells（10 格）内发现玩家即追，速度 × chase_speed_multiplier（1.35，仍略慢于玩家 160）；脱离视野 lose_sight_seconds（3 秒）后放弃，回巡逻
  * 复用 combat/state_machine.gd：enemy_patrol_state / enemy_chase_state；移动与感知在 enemy.gd 功能层
  * 性能（100 个敌人）：A* 网格由 EnemySystem 构建一次、全体共享；追击路径按 repath_interval_seconds（0.4 秒）节流重算；距玩家 > ai_active_radius_cells（32 格）的敌人休眠，完全不跑 AI
* 视野墙体遮挡（2026-09-15 用户定"加"）：
  * can_see_player() = 距离判定 + 视线采样：沿两点连线按 enemy.los_step_cells（0.35 格）采样墙格，命中任一墙即判定被遮挡（不用物理射线，100 个敌人每帧 raycast 太贵）
  * 跟丢后不再读玩家实时坐标：只走向**最后已知位置**（last known position，看到玩家时每帧刷新），走到后原地搜索，直到 lose_sight_seconds 满才放弃回巡逻
  * 总开关 enemy.vision_blocked_by_walls（true），关掉即退回纯距离判定
* 击杀掉落（2026-09-15 用户定"要掉落"）：敌人 HP 归零时按 enemy.drop.chance（75%）在原地生成一个 LootNode（复用资源点场景，缩放 0.8 与地图资源点区分）；种类按 enemy.drop.weights 加权随机（木/石/食 3、铁 2、金/油 1），数量 amount_min~amount_max（2~6，低于地图点的 10）；
  掉落物**不过期**；尸体无搜索交互
* 精英/BOSS 掉落 = **基础物资 + 随机稀有物资**（待做）
* **动态难度**（待做）：本局怪物死亡后，剩余敌人随机增强属性；**增强有上限**（2026-09-16 定，
  防止"慢杀 99 只滚出终极怪"）
* 敌人被噪音引走导致资源点无人看守 = **预期行为**
* 敌人不会破坏/拾取地图物件（后期考虑）
* 敌人之间无群体分离（会挤成一团）：后续加
* 战斗原型属性（Phase 1 临时设定，已追认见第四部分决策日志第 11 条）：
  * 敌人有 HP（enemy.max_hp=40），归零即消失（并按上述规则掉落）
  * 玩家碰到敌人 → 承受接触伤害（enemy.contact_damage=10），每敌人独立冷却（enemy.contact_cooldown_seconds=1.0）
  * 根节点由 Node2D 改为 Area2D：既可被玩家 Hitbox 检测，也能检测玩家进入
* 战争迷雾（player.vision_radius_cells = 10 格）：
  * 未探索区域：黑色遮罩
  * 已探索区域：永久揭开（探索记忆保留）；3D 版沿用同样规则
  * 敌人只在玩家当前视野半径内显示，视野外隐藏（与探索记忆无关）
  * 遮罩层 z_index 最高，撤离点在未探索区域同样不可见

## 3.7 战斗系统（Phase 1/2 原型已实现，详见 02_TECH_BUILD.md 第二部分）

* FSM 状态：Idle / Move / Attack / HitStun / Dodge / Dead（Scripts/combat）
* 攻击三段式：前摇 windup → 判定帧 active（开 Hitbox 结算）→ 后摇 recovery，
  后摇末段有 cancel_window，输入缓冲里的攻击可取消后摇直接接下一击
* 攻击判定：Hitbox（Area2D，半径 combat.attack.range_px），扇形角度
  combat.attack.arc_degrees，单次挥击每目标只命中一次，最多 max_targets 个
* 伤害管线：DamagePipeline（修饰器模式），
  FinalDmg = (Base × Πmultipliers − Defense) × RandomVariance
* 受击：HitStun 硬直 + 击退位移衰减 + 受击后短暂无敌
  （combat.player.invincible_after_hit_seconds）
* 冲刺：Dodge 朝鼠标方向位移，期间无敌帧（i-frames），退出进冷却
* 死亡：HP 归零 → Dead 状态 → RunManager.player_died()（本局物资全丢，积分保留）
* 生命上限来源：进局时取局外养成结果 Meta.get_stat("survival.max_hp")（2026-09-14 用户确认），
  config 的 combat.player.max_hp 仅作兜底默认值
* 技能系统（Phase 2 模块一，已实现）：PreCast 前摇 → Cast 生效 → PostCast 后摇 → Cooldown，
  释放即扣体力并起冷却（被打断也照扣）；三个技能在 config 的 combat.skills 节点：
  | 键 | 技能 | 机制 | 消耗 / CD |
  | :--- | :--- | :--- | :--- |
  | 1 | 蒸汽爆发 | 自身圆形 AOE（72px）+ 击退 | 25 体力 / 4 秒 |
  | 2 | 钩爪突进 | 朝鼠标方向突进，沿途穿刺（每目标一次） | 20 体力 / 5 秒 |
  | 3 | 齿轮护盾 | 2.5 秒内减伤 60% | 30 体力 / 8 秒 |
* 体力（Stamina）：上限 100，消耗后延迟 0.8 秒再以 16/秒 回复；
  释放前校验，不足则失败（指令保留在输入缓冲里，冷却/体力恢复后自动重试）
* 操作：鼠标右键 / J = 攻击，空格 = 冲刺，左键 = 移动目标，1 / 2 / 3 = 技能，H = 吃食物

### 战斗扩展决策（2026-09-15 百问百答，方向已定、待实现）
* **武器系统**：有，后续做（换武器=换攻击参数）
* **护甲**：玩家靠装备获得防御；敌人用默认值；玩家成长 = 装备 + 局外升级
* 攻击为固定值 + **有暴击**
* 多种减伤同时生效时**取最大值**（不叠乘）
* 击退**必须避免把敌人推进墙里**
* 状态效果（燃烧/中毒/泥沼）：看好不好做，暂不做
* 消耗品（药水/手雷/陷阱）：预期有
* **投掷物制造噪音引怪**：做（噪音系统的玩法延伸）
* 攻击/闪避**想加体力消耗**（当前只有技能消耗）
* 钩爪最终机制（抓取拉人？撞墙中断？跨河/裂缝？）：见「待回答」第 25 条
* 负重（背包越重越慢）：暂不加

## 3.8 手感清单（AI 实现约束）

* [x] 冲刺短暂无敌
* [ ] 伤害飘字（默认关闭，设置可开；与敌人血条同开关，见 3.10）
* [ ] 命中停顿
* [ ] 屏幕震动
* [ ] 搜刮时角色减速（针对"拆建筑/采集"，与自动拾取无关）
* [x] 撤离进度条脉冲动画

## 3.9 生存系统（食物，已实现）

* 自动进食：局内每 meal_interval_seconds（60 秒）消耗 food_per_meal（1）个食物；
  时间走局内时间轴（乘 debug.time_scale，与倒计时/撤离点同步）
* 饥饿：自动进食时背包没食物 → 进入饥饿状态，每 starvation_interval_seconds（30 秒）
  扣 starvation_damage（5）血（直接扣血，不进受击硬直、不击退）
* 主动进食：按 H（survival.eat_key）消耗 1 个食物回 heal_per_food（25）血，
  冷却 eat_cooldown_seconds（1 秒）；满血或无食物时不消耗
* HUD：左下角常驻"食物 xN + 下次进食倒计时"，饥饿时标红提示
* 实现：survival_system.gd（挂 Main 下，组 survival_system），
  通过 RunManager.consume_loot() 扣背包、Player.heal() / Player.apply_direct_damage() 改血量
* ⚠️ 食物收支平衡**尚未验证**（60 分钟自动消耗 60 个 vs 每资源点 10 单位），
  数值调整列入「待回答」

## 3.10 噪音机制（已实现，2026-09-15）

* 设计稿：02_TECH_BUILD.md 噪音一节。全局广播中心 NoiseSystem（autoload，见 project.godot）
  统一派发噪音事件，敌人接收后累加警觉度，由 FSM 阈值决定行为（疑惑 / 调查 / 狂暴）。
* 触发源（数值在 config.noise.sources）：玩家攻击(120) / 蒸汽爆发(90) / 钩爪突进(60) /
  齿轮护盾(45) / 冲刺(40) / 行走脚步(22) / 敌人咆哮(160)。
  各动作在对应状态里调用 `NoiseSystem.emit(世界坐标, 强度)`。
* 派发模型（性能：事件广播而非物理 Area2D，100 敌人同时发声不爆）：
  * 距离衰减：线性，received = 强度 × (1 − 距离 / 听力半径)；听力半径 noise.hear_radius_cells（16 格）
  * 墙体遮挡：隔墙减半 noise.wall_attenuation（0.5）；视线用网格采样（与敌人视野同源），绝不做每帧射线
  * 低于 noise.min_notice（4）的微弱噪音直接忽略
* 三档阈值（noise.thresholds，敌人警觉度 noise_alertness 累加/衰减）：
  * suspicious(15)：转黄、原地留意（不移动）
  * investigate(30)：转橙、前往声源调查（新噪音更新目标；到达后搜索，衰减到 15 以下回巡逻）
  * combat(70)：用追击速度走（表现"狂暴"）；任意时刻看见玩家直接切 Chase
* 衰减：每帧 noise.decay_per_second（10）回落，模拟"听到动静→去看→没发现→慢慢放松"
* 敌人咆哮联动：任一敌人发现玩家（进入 Chase）会 emit 咆哮噪音，惊动附近同伴 → 警报扩散；
  "我被发现了"的专属视听反馈：后续加
* 视觉反馈：① 每次噪音在声源处生成扩散圆环（半径=实际可听范围，fx_ring 灰盒）
  ② 敌人本体 Polygon2D 按状态染色（红=巡逻 / 浅黄=疑惑 / 橙=调查 / 亮红=看见玩家）
* 实现文件：noise_system.gd（广播中心）、enemy.gd（警觉度/听觉/hear_noise）、
  enemy_investigate_state.gd（调查状态）、各玩家状态接入 emit
* 后续可选增强（未做）：小地图 Tween 噪音图标、白噪音区掩蔽、动态寻路权重、程序化声学地形、
  天气对噪音的影响（见 3.1）

## 3.11 UI 与表现（2026-09-15 定）

* UI **支持中英文**双语；有**设置菜单**（音量/键位/分辨率）
* **无新手引导**
* BGM 风格：蒸汽朋克工业氛围；音效清单与优先级待定
* 小地图后续加资源/敌人/噪音标记
* 敌人头顶血条 / 伤害数字：**默认关闭，可在设置里开**
* 结算面板显示：带回资源列表、击杀数、存活时长等（明细后定）
* 死亡无回放/掉落表现
* 手柄支持：看复杂度后续再定

## 3.12 数值配置

所有数值统一放 Data/config.json，禁止硬编码。

## 3.13 中长期方向（2026-09-16 用户口述，方向性、全部待设计）

> 这两条会显著改变游戏形态，实现前必须单独出设计稿并追认。

* **RTS 发育玩法并存**：在"搜打撤"之外加入 RTS 式发展——玩家可**改造地形**、
  基地自动采集建筑与地图采集**并存**（不互相替代）。与搜打撤的动机调和方案待设计。
* **抓宠撤离**：玩家可捕捉怪物成为**可控单位**，并带它们一起从撤离点撤离。
  这解释了"撤离点只能用一次"的规则（要连部队一起带走）。捕捉手段、单位上限、
  可控单位的 AI 与噪音行为全部待设计。
* **拆建筑（搜刮）**：POI 建筑可拆，掉落/读条/噪音反馈待设计（依赖 POI 先做）。

---

# 第四部分 · 决策日志与开放问题

## 待回答（2026-09-15 百问百答 + 2026-09-16 追问遗留；已拍板方向见第三部分正文）

21. 资源 `value`（价值）字段目前没有任何消费方——是删掉，还是预留给商人/出售系统？
22. ~~死亡后重新进入同图~~ → 最终定为**「失物怪」机制**：丢失物变成特殊怪在后续对局刷新，击败收回
    （见第二部分与本表第 48 条；方案 A 种子重放、方案 B 撒掉落物均被替代）。
23. 6 种怪的**玩法差异**（机械怕什么、丧尸怕什么、各群系刷什么怪）——用户"先想想"，待设计。
24. 巢穴的具体设计（产出哪些怪、拆除奖励）——待设计。
25. 钩爪突进的最终机制（是否加抓取拉人、撞墙是否中断、能否跨河/跨裂缝）——待设计。
26. 天气系统数值：雨/雷对噪音的具体系数、切换频率、**HUD 是否显示天气预报**、
    改变天气的道具种类——待设计（已定：局内动态切换 + 有道具）。
27. 积分细则（已定：死亡低分入账、撤离翻倍）：杀怪各分值？生存给分规则（每分钟 +N 还是
    活到撤离一次性）？哪些升级项用资源、哪些用积分（雕像双货币页签？）——待设计。
28. 撤离速度升级的最低下限（站圈秒数能压到几秒）——待定。
29. "资源发现概率"升级具体改哪个参数（loot.density？）与上限——待定。
30. 旧存档的版本迁移策略（等级是否重置）——待定。
31. 结算面板明细最终展示哪些字段——"后面再看"。
32. 音效清单与优先级——"后面看"。
33. 手柄支持——看复杂度再定。
34. 3D 版敌人共享 A* 网格方案是否已迁移——**待代码核实**。
35. 渲染后端最终锁 gl_compatibility 还是切 Forward+——待定。
36. 最低配置目标——待定。
37. 被打断技能"照扣体力"是否需要可见反馈（音效/飘字）——待定。
38. 撤离成功瞬间"拾取冷却重试中"的资源如何结算——换种问法：
    背包满时走进资源点会拾取失败并 0.5 秒后重试；若恰好此时撤离读条完成，
    这 0.5 秒窗口内的资源点算捡到了还是没捡到？（建议：以进圈瞬间背包内容为准，之后不再结算）
39. 食物收支平衡：60 分钟自动消耗 60 个食物 vs 资源点每个 10 单位，数值是否要调？
48. 「失物怪」（死亡回收机制）细则待设计：
    ① 刷新时机——死亡后**下一局必刷**，还是每局按概率刷（越晚找回越难=惩罚递增）？
    ② 多次死亡——各刷一只（背包里躺着 N 只怪的历史），还是合并成一只"驮山怪"？
    ③ ~~行为~~ → 已定：随机两种状态——怯懦型（HP≤50% 逃跑）/ 凶悍型（HP>50% 逃跑、
    ≤50% 反战）。遗留：逃跑时是否往玩家反方向/资源点方向跑？逃跑是否受地形（过河减速）影响？
    强度倍率？
    ④ ~~掉落~~ → 已定：掉血随机掉落（每损失 N% HP 掉一份，击败掉全部剩余）。
    遗留：掉落阈值 N 取多少（10%？20%？）；掉出的遗失物在地上会不会再被别的怪踩丢/有过期时间；
    掉落瞬间是否也产生噪音（血包噼里啪啦掉一地，引来围观）。
    ⑤ 未击败的失物怪是否永久保留（跨局累积）？上限几只？
    ⑥ 是否占每局 100 敌人配额、是否触发动态增强；视觉识别（建议蒸汽朋克"驮箱傀儡/搬夫"主题）。
49. 6 种怪与地形/群系的**刷新映射表**（哪个群系刷哪派、各几种）——后定。
50. 下蹲/静走状态：噪音降到多少？是否消耗体力/减速？按键？——待设计。
51. 保底初始装：具体包含什么（武器？护甲？食物几个）？死亡后每次回基地自动补发？——待设计。
52. 抓宠撤离 / RTS 地形改造：均为大方向，需要单独设计稿（见 3.13）；先确认优先级——
    排在 Phase 几？
53. Steam 成就清单 + 云存档与多档位存档的结构冲突（云同步一般单档，需预留 per-slot 上传策略）。

## 已回答（决策日志，压缩为一行索引；细节均已同步进第三部分正文）

1. 失败惩罚：死亡与超时相同，丢失全部未撤离资源。（run_manager.gd）
2. 撤离点关闭规则：60 分钟四阶段——0-30 无；30 分开 3 个；45/55 各关 1 个；最后一个开放至超时。（extraction_system.gd）
3. 局内时长：60 分钟（原 20 分钟作废）。
4. 地图连通性：洪水填充 + 可达率不足自动重生成（30%/10 次）+ 内容只刷可达格；封闭小房间允许存在作为探索风险。
5. 搜刮资源 6 种（木/石/铁/金/油/食）；每点 10 单位；撤离自动入库；仓库 20 格/叠加 1000/可升级。
6. 局内背包：格子制（每种 1 格），默认 10 格可升级。
7. 战斗系统语言：不采用蓝图的 C#/C++，继续 GDScript。
8. 移动控制：保留鼠标点击寻路，不用 WASD 八向；FSM 包裹 Idle/Move。
9. 交付粒度：战斗按蓝图分模块交付，Phase 1 只交 FSM 框架。
10. 单位口径：视野/距离按 1 瓦片格（16px）实现（视野 10 格、生成距离 20 格）。
11. 敌人战斗属性：HP=40、接触伤害=10（独立 1 秒冷却）、根节点改 Area2D。
12. 敌人 AI：巡逻 + 追击（不做完整四状态机）；远处休眠、A* 共享、路径节流。
13. 食物用途：战斗资源——每 60 秒自动吃 1 个，H 吃 1 个回 25 血，断粮掉血。
14. 地图最终尺寸：当前不关注、后续再定（原型 128x128）。
15. 升级货币：旧存档废铁/蒸汽核心属废弃数据，读档自动剔除。
16. 局外 max_hp 进局内：max_hp = Meta.get_stat("survival.max_hp")，config 仅兜底。
17. 三个技能组"先用着"：蒸汽爆发/钩爪突进/齿轮护盾，数值全在 combat.skills。
18. 敌人视野墙体遮挡：视线采样（0.35 格步长）+ 跟丢走最后已知位置；开关 vision_blocked_by_walls。
19. 敌人击杀掉落：75% 原地掉 LootNode（缩放 0.8），加权随机 2~6 单位。
20. 噪音机制已实装：NoiseSystem 广播 + 三档阈值 + 调查状态 + 衰减 + 咆哮扩散 + 视觉反馈。
21-39. 2026-09-15 百问百答：见第三部分正文（3.1/3.2/3.4/3.5/3.6/3.7/3.10/3.11 及第二部分）。
40. 2026-09-16 追问：节奏=前潜行后战斗（+下蹲静走状态待做）；难度靠撤离点阶段控制（45 分前易）。
41. 死亡回收定为「失物怪」：丢失物变特殊怪在后续对局刷新，掉血随机掉落遗失物（击败掉全部剩余，
    不必击杀）；不做整局快照/重进。
42. 积分：死亡低分入账、撤离翻倍（防送死变现）；细则见待回答 27。
43. 基地保底发放初始装，防穷死循环；安全箱与背包格子不冲突。
44. 阵营互殴=谁近打谁；动态增强有上限；资源点总量刻意压低（惊喜定位）。
45. 天气局内动态切换 + 道具可改变天气。
46. 撤离点一次性=为"抓宠撤离"（可控单位随玩家撤离）铺路；中长期 RTS 发育/改造地形并存（见 3.13）。
47. Steam：要成就 + 云存档（结构冲突见待回答 53）。内容量问题：不焦虑，慢慢加（项目第二天）。
