# 04_OPEN_QUESTIONS.md — AI 疑问清单

> 规则：AI 发现设计不清楚时，写入本文件，不要擅自决定。
> 我（人类）会定期回答，回答后移入对应文档。

## 待回答

（暂无）

## 已回答

1. 失败惩罚：死亡与超时相同，丢失全部未撤离资源。（已同步至 01/02，实现于 run_manager.gd）
2. 撤离点关闭规则：一局 60 分钟四阶段——0-30 无撤离点；30 分钟开 3 个；45/55 分钟各随机关闭 1 个；最后一个开放至超时。（已同步至 01/02，实现于 extraction_system.gd + config 的 extraction 节点）
3. 局内时长：60 分钟（原 20 分钟决定作废）。（已同步至 01/02/config.json）
4. 地图连通性：要求出生点与撤离点之间必然走通。方案=洪水填充可达性分析 + 可达率不足自动重生成（min_reachable_ratio=30%，最多重试 10 次）+ 撤离点/敌人只刷在可达格。（已实现并同步至 02 第 1 节；封闭小房间允许存在，作为探索风险，但内容物不会刷在里面）
5. 搜刮资源（2026-09 用户定）：6 种——木头/石头/铁/金币/石油/食物，颜色占位；每点拾取 10 单位；撤离自动入仓库；仓库默认 20 格、每种叠加上限 1000、可升级。（已实现：loot/storage 节点 + loot_system/loot_node/run_manager/meta_progression，同步至 01/02）
6. 局内背包：格子制（每种物品占 1 格），沿用 backpack_capacity 升级线（默认 10 格）。
7. 战斗系统语言：06_FIGHT.md 蓝图的"使用 C#（Unity）或 C++（UE）"约束**不采用**，项目继续用 GDScript（与 Godot 4 技术栈一致）。（2026-09-14 用户确认）
8. 移动控制取舍：保留鼠标点击寻路，不用蓝图 Phase 1 的"WASD 八向移动"。FSM 的 Idle/Move 状态包裹现有点击寻路。（2026-09-14 用户确认）
9. 交付粒度：战斗系统按蓝图要求分模块交付，Phase 1 先只交 FSM 框架，后续模块逐个交付。（2026-09-14 用户确认）
10. 单位口径：视野/距离的单位按 1 瓦片格（16px）实现（视野半径 10 格、敌人生成距离 20 格）。（2026-09-14 用户确认"没问题"）
11. 敌人战斗属性：HP=40、接触伤害=10（每敌人独立 1 秒冷却）、被打到 0 即消失且暂不掉落；敌人根节点由 Node2D 改为 Area2D 以便被玩家 Hitbox 检测和触发接触伤害。（2026-09-14 用户追认"可以"）
12. 敌人 AI（2026-09-14 用户定）：做到**巡逻 + 追击**（不做完整四状态机）。已实现
    enemy_patrol_state / enemy_chase_state + enemy.gd 感知与寻路功能层；远处敌人休眠、
    A* 网格共享、路径节流重算（性能保护）。已同步至 02 第 6 节。
13. 食物用途（2026-09-14 用户定）：食物是**战斗用的**——局内每 60 秒自动消耗 1 个，
    并且可以吃来回血。已实现 survival_system.gd：自动进食 / 饥饿每 30 秒扣 5 血 /
    按 H 吃 1 个回 25 血（冷却 1 秒）。已同步至 01 资源表与 02 第 9 节。
14. 地图最终尺寸：用户表示**当前不关注、后续再定**——原型保持 128x128，该问题关闭。
15. 升级货币：旧存档的废铁（scrap）/ 蒸汽核心（steam_core）属废弃数据，用户确认不再保留。
    已实现 Meta 读档时自动剔除 config.resources 之外的资源并回写存档。
16. 局外 max_hp 进局内（2026-09-14 用户确认"进"）：进局时 max_hp =
    Meta.get_stat("survival.max_hp")（雕像升级结果），config 的 combat.player.max_hp 仅作兜底。
    已实现于 player.gd 的 _init_combat。
17. 三个技能组（2026-09-15 用户回复"先用着"）：暂定技能组**追认沿用**，后续手感不对再调。
    1 蒸汽爆发（自身圆形 AOE 72px + 击退，30 伤害 / 25 体力 / CD 4 秒）、
    2 钩爪突进（朝鼠标突进沿途穿刺，20 伤害 / 20 体力 / CD 5 秒）、
    3 齿轮护盾（2.5 秒减伤 60%，30 体力 / CD 8 秒）。
    按键 1/2/3，全部数值在 config.json 的 combat.skills，改配置即可不动代码。
18. 敌人视野墙体遮挡（2026-09-15 用户定"加"）：can_see_player() = 距离判定 +
    视线采样（沿连线按 enemy.los_step_cells=0.35 格采样墙格，命中即遮挡）。
    跟丢后不再追玩家实时坐标，只走向**最后已知位置**（last known position），
    走到后原地搜索直到 lose_sight_seconds 放弃回巡逻。开关 enemy.vision_blocked_by_walls。
19. 敌人击杀掉落（2026-09-15 用户定"要掉落"）：死亡按 enemy.drop.chance=75% 在原地
    生成一个 LootNode（复用资源点场景，缩放 0.8 便于区分），种类按 enemy.drop.weights
    加权随机（木/石/食 3、铁 2、金/油 1），数量 2~6（低于地图资源点的 10）。
    已实现于 enemy.gd 的 _die/_spawn_drop + loot_node.gd 的 setup(res_id, amount, scale)。
20. 噪音机制（2026-09-15 用户定"开始做"）：06_FIGHT.md 第 8 节设计稿**已实装**。
    全局广播中心 NoiseSystem（autoload）+ 敌人 noise_alertness 三档阈值（suspicious 15 /
    investigate 30 / combat 70）+ enemy_investigate_state（前往声源调查）+ 衰减（10/秒）
    + 敌人咆哮警报扩散 + 视觉（声源扩散圆环 + 敌人本体染色）。玩家攻击/冲刺/技能/脚步均发声。
    数值全部在 config.noise 节点。已同步至 02 第 10 节、05 文件树、06 第 8 节。
    后续可选增强（未做）：小地图 Tween 噪音图标、白噪音区掩蔽、动态寻路权重、程序化声学地形。
