# 03_ART_STYLE_GUIDE.md — 美术风格约束

## 参考对象
《They Are Billions》

## 核心原则
**清晰度优先**：简单线条、明确界面、蒸汽朋克色调。

## 色调
- 暗棕（主背景）
- 铜锈色（建筑/机械）
- 蒸汽白（UI/高光）
- 警示橙/红（危险/敌人）

## 视觉要点
- 角色和建筑做轮廓强化，暗色背景下依然清晰可辨
- UI 走"工业仪表盘"风格
- 敌人群体：不追求精致，但数量感要出来

## 生成规则
1. 每次生成素材前，先阅读本文件。
2. 一次只生成一个类别：角色 → 建筑 → 敌人 → UI。
3. 生成 prompt 必须包含本文件的色调和风格关键词。
4. 生成后人工筛选，不一致的丢弃。

## 素材规格（2026-09-14 定）

### 形式与视角
* **3D 建模 → 2.5D 斜俯视渲染成精灵图**。3D 模型只作出图源，不直接进引擎（项目是 2D / Node2D）。
* 相机俯角约 50-60°，接近《They Are Billions》的等距感：能看到角色头顶、肩部与正面，暗背景下轮廓清晰。
* 光源统一为左上偏冷的主光 + 铜锈色环境反射，角色脚下留短投影以增强"贴地感"。

### 朝向与帧数
* **4 向**：下 / 上 / 左 / 右（左右允许镜像）。
* 每向基础帧：待机 4 帧、移动 4 帧、攻击 3 帧（前摇 / 判定 / 后摇各 1）、受击 1 帧、冲刺 1 帧。
* 首批先出**静态基准帧**（待机 / 攻击 / 冲刺），动画帧后续逐批补。
* 当前实现（2026-09-14）：只有待机基准帧，动作用**代码驱动的程序化动画**——走路上下起伏+摆动、攻击前冲+摆臂、受击抖动泛红、冲刺沿朝向拉伸+半透明、死亡倾倒变暗（`player.gd _update_sprite`，按 FSM 状态名分支）。真动画帧补齐后改接 `AnimatedSprite2D` + `SpriteFrames`。

### 尺寸与导入
* tile = 16px；角色视觉高度约 24px（约 1.5 格），宽度约 16-20px。
* 渲染画布 **48×48**（2 倍超采样），导出 PNG 透明底。
* Godot 导入：保持默认 `filter = Linear`、mipmap 关闭。理由：2.5D 平滑渲染素材不是像素风，Nearest 在非整数缩放下会出现锯齿和抖动。
* 精灵锚点=脚底中心（Sprite2D：`scale = 0.5` 显示、`offset = (0, -画布半高)`），逻辑位置即脚底。
* **素材放好后必须让 Godot 导入一次**（新 PNG 未导入时 `preload` 会让脚本加载失败、角色直接消失）：
  菜单「项目 → 工具 → 扫描文件系统」，或命令行 `godot --headless --path <项目> --import`。
* 只作源材料、不进引擎的目录（`Raw/` 原始大图、`Source3D/` 3D 模型）放一个 `.gdignore`，Godot 会跳过整个目录。

### 命名规范
`player_{action}_{dir}_{frame}.png`，例：`player_idle_down_00.png`、`player_attack_right_01.png`

## 地形美术（2026-09-15 定：AI 贴图 + 程序化布局）

### 分工原则
* **代码管布局**：生物群系分区、植被成簇、密度、噪声、寻路 —— 参数化、零成本、可复现（`Scripts/map_generator.gd`）。
* **贴图管画面**：地面/墙体/装饰物的笔触质感 —— 走 AI 生成。纯 `Image` 逐像素绘制有质感天花板，到不了《They Are Billions》的笔触级精致度（TAB 本身就是「程序化布局 + 预渲染贴图集 + 光照/雾效」，精致度是资产问题、不是引擎问题）。

### 地面与墙体：纹理采样成图集
* AI 生成 **1024×1024 无缝纹理**。prompt 必须包含：`seamless tileable ground texture`、`hand-painted pixel-art style, crisp chunky pixels`、`perfectly flat top-down view with no perspective`、`even flat lighting, no object shadows, no vignette, no border, texture fills the entire frame edge to edge`，以及本文件色调关键词（暗棕 / 铜锈 / 蒸汽白 / 去饱和）。
* 离线按 16px 网格从纹理上采样互不重叠的方块作为变体，拼成横向一行图集。
* **每个采样瓦片必须做亮度与对比度归一化**：直接把图不同位置的方块拼起来，各块明暗天生不同，铺到地图上会出现明显的"棋盘格"拼贴感；统一到全图均值 + 对比度压到 0.82 后，只保留内容差异（这格是裂纹、那格是苔藓），格子感消失。
* **墙体额外压暗 20%**：金属板纹理在 16px 下会被平均成亮灰蓝块，与地表反差过大，压暗后衔接自然。
* 图集布局（横向一行，y 恒为 0，共 96 列）：
  * 列 0-47 → 地板，4 群系 × 12 变体（`atlas_floor.png`，768×16）
  * 列 48-71 → 墙体，4 群系 × 6 变体（`atlas_wall.png`，384×16）
  * 列 72-95 → **墙顶受光变体**，由墙体列程序化派生（整体提亮 1.10 + 首行高光边），省一份素材且光照关系天然一致
* 变体由格子坐标哈希确定性挑选（`_variant`），重生成地图不会闪烁。

### 装饰物：抠图成透明精灵
* AI 直出单物件原画（俯视 3/4 角），**但 `background: transparent` 参数实测不生效**，会出白底/浅灰底。
* 抠图按素材对比度分两条通道：
  * **高对比度素材**（树、残骸：物件本身极暗，背景纯白）→ 全局「最暗通道亮度」软阈值。不要用连通域洪水填充：树冠会把部分白底"包围"导致填充传不进去，留下白斑。
  * **低对比度素材**（石头：背景浅灰渐变 + 投影，与青灰石头最暗通道只差 20）→ 亮度阈值必然失败，改用 **image-to-image 只把背景换成纯品红 `#FF00FF`**，再做 chroma key（顺带要清理工具水印）。
* 包围盒**不能用 `getbbox()`**：残留的极低 alpha（1~10）像素会把整张 1024×1024 算成内容，缩放后物件变得极小；改用 `alpha > 96` 求实心范围。
* 尺寸：树 48×36（约 3 格宽）、石头 30×22、残骸 26×22。

### 运行时丰富度
每株装饰随机缩放 0.88-1.12 / 水平翻转 / 亮度 0.88-1.10，并按**所在群系色调**偏色（荒原偏枯黄、锈泽偏湿绿、石原偏冷灰），避免克隆感。

### 工具脚本
`tools/build_tile_atlas.py`（纹理 → 瓦片图集）、`tools/make_decor_sprites_final.py`（原画 → 透明精灵）。
贴图缺失或未导入时，`map_generator.gd` 会自动回退到程序化逐像素绘制，工程始终可跑。

## 观感调优（2026-09-15 二轮：从"丑"到"能看"）

一轮贴图接入后地图仍然"丑"，根因**不在贴图质量，在渲染范式**。失分点按影响排序：

| # | 问题 | 层级 |
|---|---|---|
| 1 | 正交俯视、整图无高度 → 物件像贴纸 | 范式级（待做：斜俯视 + 立体墙） |
| 2 | 零光照零大气 → 全图均匀亮 | 范式级（待做：光照层 + 雾 + 暗角） |
| 3 | 群系边界是像素台阶 | 调优级（已解决） |
| 4 | 16px 瓦片重复规律肉眼可见 | 范式级（待做：瓦片放大到 32） |
| 5 | 配色脏、无视觉焦点 | 调优级（已解决） |

已落地的调优，全部走 `Data/config.json` 的 `map.*`，改参数即可复现：

* **群系边界双保险**：`biome_border_frequency/jitter` 用中频噪声把边界打散成犬牙；`biome_edge_blend` 再把交界格按概率改判为邻格群系，让两块区域互相"咬"进去。**只在生成阶段做**——放到渲染时临时算，`biome` 数组与实际渲染、装饰物偏色就会不一致。
* **群系分布均衡**：`biome_spread` 必须 >1。simplex 噪声值集中在 0 附近，直接 `(n+1)/2` 分段会让中间两段吃掉近九成面积（实测 590/7763/7344/687），扩幅到 1.35 后回到约 4000±1000。
* **色调分级** `grade`：对比曲线 → 去饱和 → 群系色调，**顺序不可换**。对比把灰糊的中间调拉开；去饱和 0.75 是"艳"的解药；最后才乘 `BIOME_TINT` 拉开四区明暗。
* **调参铁律：主要动明度，色相只做轻微偏移。** 第一版把荒原写成 `(1.18, 1.07, 0.82)` 这种强暖偏移，叠在原本就偏棕黄的贴图上整片变成饱和橙黄，比"脏"更糟。同理石原给到 `(0.94, 0.95, 0.98)` 会像冰面——它的原贴图本身就是亮冷灰石板，**只能压暗，不能加冷**。
* **宏观明暗层** `macro_light`：TileMapLayer 做不到单格 modulate，改为生成与地图等格数的低分辨率灰度图（每格 1 像素），用 `BLEND_MODE_MUL` 放大混合，线性过滤天然平滑又省显存。**必须最后添加**，否则会出现"地面有明暗、树却一样亮"的割裂。
* **装饰落地投影** `decor.shadow`：椭圆软影，先添加影子再添加本体，同 y 下 `y_sort` 保持添加序，影子就永远压在本体下面。残骸本来平摊在地面，不投影。

### 已知遗留（留给后续）
* 群系之间仍是"硬内容切换"，没有过渡瓦片；边界靠渗透缓解，不是真正的 autotile 过渡带。
* 石原是视觉上最跳的一块，根因在 AI 原贴图（亮冷灰石板 + 青绿苔痕），调色只能缓解，换素材才能根治。

## 素材清单（待填充）
- [x] 玩家角色（2026-09-14：四向待机基准帧完成，3D 源模型存 Assets/Art/Source3D；动画帧待补）
- [x] 地形瓦片（2026-09-15：4 群系地板 + 墙体 + 墙顶，AI 无缝纹理采样成图集）
- [x] 地形装饰物（2026-09-15：树 / 石头 / 残骸，AI 原画抠图为透明精灵）
- [ ] 普通敌人
- [ ] 精英敌人
- [ ] 资源点
- [ ] 撤离点
- [ ] 建筑/掩体
- [ ] UI 图标
- [ ] 升级界面
